[user]
    name = Daniel Cohen
[color]
    ui = auto
[core]
    excludesfile = ~/.gitignore_global
[alias]
    graph = log --graph --decorate --oneline --all
    ci = commit -a -m 
    st = status
    co = checkout
    br = branch
    psafe = pull --ff-only
[include]
    path = .gitconfig.local
